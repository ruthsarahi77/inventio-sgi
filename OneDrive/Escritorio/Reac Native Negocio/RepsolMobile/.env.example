# Spring Boot origin, without /api. Android emulator:
EXPO_PUBLIC_API_URL=http://10.0.2.2:8080
# Physical device: http://<LAN-IP-of-your-computer>:8080
# Expo Web: http://localhost:8080 (allow its origin in backend CORS_ALLOWED_ORIGINS)
# Never put database passwords, JWT_SECRET or Supabase keys here.
